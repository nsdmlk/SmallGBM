# SmallGBM
SmallGBM — gradient boosting optimized for small datasets (n &lt; 1000). XGBoost and LightGBM fail here; SmallGBM wins with aggressive regularization and minimal tree depth. scikit-learn compatible, pip installable, built from scratch in Python.
